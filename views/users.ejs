<%- include('includes/head.ejs') %>
    <h1>Users</h1>
    <% if (users.length > 0) { %> 
        <ul>
            <% user.forEach((user) => { %>
                <li><%= this.name %></li>
            <% }) %>
        </ul> 
        <% else { %>
            <h1>No Users Found!</h1>
    <% } %>
<%- include('includes/end.ejs') %>